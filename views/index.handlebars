    <div class="div-view">
        <div>
            <link rel="stylesheet" href="/styling/index.css"/>
        </div>

        <div>
            <h1>Home Page</h1>
        </div>

        <div class = "Center">
            <a href="/view_teams"><button>Log In</button></a>
        </div>

        <br><br>
        <div id="teamsTable">
            <table>

                <thead>
                    <tr>
                        <th>Team Name</th>
                        <th>Team Leader</th>
                        <th>Team Members</th>
                    </tr>
                </thead>

                {{#each tableData}}
                    <tr>
                        <td>{{this.team_name}}</td>
                        <td>{{this.scrum_master}}</td>
                        <td>{{concat_members this.member_names}}</td>
                    </tr>
                {{/each}}

            </table>
        </div>
        
    </div>