 
 <body>
     <div>
            <link rel="stylesheet" href="/styling/index.css"/>
    </div>
    <div class = 'center' id="teamsTable">
        <br><br>
        <table>

            <thead>
                <tr>
                    <th>Team Name</th>
                    <th>Team Leader</th>
                    <th>Team Members</th>
                    <th></th>
                </tr>
            </thead>

            {{#each tableData}}
                <tr>
                    
                    <td>{{this.team_name}}</td>
                    <td>{{this.scrum_master}}</td>
                    <td>{{concat_members this.member_names}}</td>
                    <td>
                        <form method="POST" action="/team_portal">
                            <input type="hidden" 
                            id = "team_name"
                            name="team_name"
                            value="{{this.team_name}}"
                            >
                            <input id="butt" type="submit"
                            name="submit"
                            value="Team Portal">                    
                        </form>
                        
                    
                    </td>
                </tr>
            {{/each}}

        </table>
    </div>
</body>

